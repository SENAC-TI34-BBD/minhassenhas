import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: 54,
    backgroundColor: "#FFFFFF",
    borderRadius: 7,
    padding: 16,
    marginBottom: 16,
    flexDirection: "row",
  },
  input: {
    color: "#000",
    flex: 1,
  },
});
