import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    width: "100%",
    height: 54,
    backgroundColor: "#FFFFFF",
    borderRadius: 7,
    padding: 16,
    marginBottom: 16,
  },
});
