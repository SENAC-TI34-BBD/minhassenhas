import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        width: "100%",
        height: 54,
        backgroundColor: "#CECECE",
        borderRadius: 7,
        color: "#000",
        padding: 16,
        marginBottom: 16,
    }
});